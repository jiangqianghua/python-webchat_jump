# python-webchat_jump
python编写微信跳一跳外挂
本人是mac环境下，如果是在window环境下，需要把 #screenshot = screenshot.replace(b'\r\n', b'\n') 这句代码打开，代码中有注释
环境要求：
 安装adb，配置环境变量
 android手机以及可以调试的usb接口，
 android手机需要打开debug调试
系统会自动检测手机分辨率，然后根据分辨率读取对应的外挂配置信息
